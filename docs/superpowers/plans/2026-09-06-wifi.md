# Wi-Fi 6+ — Plano de Implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Entregar oito labs de Wi-Fi 6+ para o módulo `comms/`, cobrindo os três itens da súmula (circuito companion, provisionamento, socket TCP/IP) mais TWT, coexistência BLE+Wi-Fi e locationing, todos validados no nRF54LM20-DK variante B + nRF7002-EB II.

**Architecture:** Cada lab é uma app *freestanding* em `comms/`, no mesmo formato dos cinco labs de Channel Sounding: cópia literal do SDK com cabeçalho `ORIGEM:` e divergências marcadas, ou código do curso quando não há sample. Os labs 9, 10 e 13 compartilham um payload JSON de uma linha; o transporte (TCP/HTTP/MQTT) é escolhido em Kconfig no mesmo firmware. Ferramentas de PC em Python com `pytest`, no padrão de `comms/05_cs_iq_music/tools/`.

**Tech Stack:** nRF Connect SDK v3.4.0, Zephyr, shield `nrf7002eb2`, snippet `nrf70-wifi`, sysbuild; Python 3.11 (`pytest`, `requests`, `protobuf`, `paho-mqtt`); PPK2; TP-Link EX3000.

**Spec:** `docs/superpowers/specs/2026-09-06-wifi-design.md`

## Global Constraints

- **Board target:** `nrf54lm20dk/nrf54lm20b/cpuapp` (variante **B**). Nunca a variante A.
- **Shield e snippet, sempre escopados por imagem no sysbuild:** `-D<app>_SHIELD="nrf7002eb2"` e `-D<app>_SNIPPET=nrf70-wifi`.
- **Blobs:** `west blobs fetch nrf_wifi` precisa ter rodado. Já foi feito nesta bancada; o `PREREQUISITOS.md` documenta.
- **Console na `uart30`** com o shield acoplado — **outra VCOM** (medido nas duas condições:
  sem shield na segunda, com shield na primeira). A `uart20` some do `device list`. Todo README
  abre com esse aviso.
- **`sw3` não existe** com o shield: só `sw0`–`sw2` e os quatro LEDs.
- **Toda cópia do SDK** leva cabeçalho `ORIGEM:` no topo (SDK, Upstream, Local, Copiado, linha de `diff`, e a lista `DIVERGENCIA DO CURSO`), e cada mudança marcada com `ALTERADO PELO CURSO (nrf-manaus-2)`.
- **`LICENSE`** em cada pasta de lab, cópia byte a byte de `comms/05_cs_iq_music/LICENSE`.
- **Sem acentos** em comentários de código e em mensagens de CMake; READMEs em português com acentos normais.
- **`minha_rede.conf`** é rastreado e **vazio** no repo. Nunca commitar SSID/senha reais.
- **READMEs descrevem o estado final** — sem histórico de decisões descartadas.
- **Commits:** mensagem em português, corpo explicando o porquê, trailers
  `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>` e
  `Claude-Session: https://claude.ai/code/session_01LurGbU8nMm9xZDvXheodHb`.
- **Firmware não tem teste unitário.** Tarefas de firmware fecham com *build limpo* + *checklist de bancada* explícito. Só as ferramentas de PC têm `pytest`.

---

## Estrutura de arquivos

```
comms/
  06_wifi_shell/          cópia de nrf/samples/wifi/shell        (sem divergência)
  07_wifi_sta/            cópia de nrf/samples/wifi/sta          (+ minha_rede.conf, CMake fail)
  08_wifi_provisioning/   cópia de nrf/samples/wifi/provisioning/softap
  09_wifi_tcp/            código do curso (esqueleto do sta)
    src/main.c            conexão + telemetria + botão + LED
    src/payload.c/.h      monta a linha JSON  (única fonte do payload)
    src/transporte.c/.h   TCP | HTTP | MQTT atrás de uma interface
    Kconfig               LAB_TRANSPORTE, LAB_SERVIDOR_IP, LAB_PORTA, LAB_INTERVALO_MS
    tools/wifi_server.py  servidor TCP + comando de LED
    tools/tests/          pytest do servidor e do parser
  10_wifi_http_mqtt/      SEM firmware — README + tools/ (recompila o 09)
    tools/wifi_http_server.py
    tools/wifi_mqtt_sub.py
  11_wifi_twt/            cópia de nrf/samples/wifi/twt          (+ minha_rede.conf)
  12_wifi_coex/           cópia de nrf/samples/wifi/ble_coex     (+ minha_rede.conf)
  13_wifi_location/       código do curso
    src/main.c            scan + monta lista de APs + envia por TCP
    tools/wifi_locate.py  resolve a posição no nRF Cloud
    tools/tests/
```

**Por que o lab 10 não tem firmware:** ele recompila o do lab 9 com outro
`CONFIG_LAB_TRANSPORTE`. Duplicar ~400 linhas de C para trocar o transporte contradiria a tese
do bloco ("um payload, três transportes") e criaria duas cópias para manter. Mesmo padrão do
`04_cs_seguranca`, que também é só roteiro.

---

### Task 1: Teste dos cinco minutos do TWT — decide se o lab 11 existe

Roda no dia em que o EX3000 chegar. Se falhar, o roteador volta dentro do prazo de devolução e
o lab 11 sai do escopo. **Nenhuma outra tarefa depende desta** — as demais podem começar antes.

**Files:**
- Create: `comms/11_wifi_twt/NOTAS_BANCADA.md` (registro do resultado)

**Interfaces:**
- Consumes: nada.
- Produces: a decisão binária "lab 11 existe / não existe", registrada em `NOTAS_BANCADA.md`.

- [ ] **Step 1: Configurar o EX3000**

Conectar por cabo, abrir `http://192.168.0.1` (ou `http://tplinkwifi.net`), definir SSID
`nrf-curso` e senha WPA2. Ir em **Advanced → Wireless → Wireless Settings** e procurar
**TWT**. Se existir, habilitar; se não existir o toggle, seguir assim mesmo e anotar.

Anotar em `NOTAS_BANCADA.md`: versão de firmware do roteador (**System → Firmware Upgrade**),
e se o toggle de TWT existe.

- [ ] **Step 2: Gravar o shell da Nordic direto do SDK**

```bash
cd /c/ncs/v3.4.0
nrfutil sdk-manager toolchain launch --ncs-version v3.4.0 -- \
  west build -p -b nrf54lm20dk/nrf54lm20b/cpuapp --sysbuild \
  -d /tmp/twt_probe nrf/samples/wifi/shell \
  -- -Dshell_SHIELD="nrf7002eb2" -Dshell_SNIPPET=nrf70-wifi
nrfutil device program --firmware /tmp/twt_probe/shell/zephyr/zephyr.hex \
  --serial-number 1051898754 --options reset=RESET_SYSTEM
```

- [ ] **Step 3: Achar o console e conectar**

Com o shield acoplado o console está na `uart30` — **outra VCOM**. Abrir as duas COM da DK e
ver qual responde. No prompt `uart:~$`:

```
wifi scan
wifi connect -s nrf-curso -k 1 -p <senha>
wifi status
```

Esperado: `Status: COMPLETED`, banda e canal preenchidos.

- [ ] **Step 4: O teste que decide**

```
wifi twt setup 0 0 1 1 0 1 1 0 0 512 2621440
wifi status
```

Esperado no sucesso: log de `TWT` aceito (`TWT response: ... accept`) e, no `wifi status`, o
modo de power save indicando TWT.

**Se a resposta for reject/timeout:** o AP não negocia TWT individual. Devolver o EX3000 e
registrar. O lab 11 sai do escopo e o §8 da spec é atualizado.

- [ ] **Step 5: Registrar e commitar**

Escrever em `comms/11_wifi_twt/NOTAS_BANCADA.md`: data, firmware do roteador, se havia toggle,
o comando exato usado, a resposta recebida, e o veredito.

```bash
git add comms/11_wifi_twt/NOTAS_BANCADA.md
git commit -m "11_wifi_twt: resultado do teste de TWT no TP-Link EX3000"
```

---

### Task 2: Lab 6 — `wifi/shell`, o companion visível

**Files:**
- Create: `comms/06_wifi_shell/` (cópia de `nrf/samples/wifi/shell`), `README.md`, `LICENSE`, `.gitignore`

**Interfaces:**
- Consumes: nada.
- Produces: a convenção de cabeçalho e de aviso de console que os labs 7, 8, 11 e 12 repetem.

- [ ] **Step 1: Copiar o sample**

```bash
mkdir -p comms/06_wifi_shell
cp -r /c/ncs/v3.4.0/nrf/samples/wifi/shell/* comms/06_wifi_shell/
rm -rf comms/06_wifi_shell/build*
cp comms/05_cs_iq_music/LICENSE comms/06_wifi_shell/LICENSE
```

- [ ] **Step 2: Cabeçalho ORIGEM no `prj.conf` e no `CMakeLists.txt`**

No topo de `comms/06_wifi_shell/prj.conf`, antes de qualquer linha de config:

```
#
# ORIGEM: copia de arquivo do SDK — nao e codigo do curso.
#   SDK     : nRF Connect SDK v3.4.0
#   Upstream: nrf/samples/wifi/shell/prj.conf
#   Local   : C:/ncs/v3.4.0/nrf/samples/wifi/shell/prj.conf
#   Copiado : 2026-09-06 — curso nrf-manaus-2, modulo comms/06_wifi_shell
#
# Para conferir se divergiu do SDK:
#   diff <este arquivo> C:/ncs/v3.4.0/nrf/samples/wifi/shell/prj.conf
#
# DIVERGENCIA DO CURSO: nenhuma.
#
```

Mesmo bloco no `CMakeLists.txt`, com o caminho correspondente.

- [ ] **Step 3: `.gitignore` do lab**

```
# Diretorios de build
build*/
```

- [ ] **Step 4: Compilar**

```bash
cd /c/ncs/v3.4.0
nrfutil sdk-manager toolchain launch --ncs-version v3.4.0 -- \
  west build -p -b nrf54lm20dk/nrf54lm20b/cpuapp --sysbuild \
  -d C:/work/nrf-manaus-2/comms/06_wifi_shell/build_lm20 \
  C:/work/nrf-manaus-2/comms/06_wifi_shell \
  -- -D06_wifi_shell_SHIELD="nrf7002eb2" -D06_wifi_shell_SNIPPET=nrf70-wifi
```

Esperado: `exit 0`, e o resumo de memória impresso. Anotar FLASH/RAM para o README.

> Se o sysbuild reclamar do nome da imagem, usar o nome que o próprio build reporta em
> `--- <nome> ---` no log e ajustar o prefixo de `_SHIELD`/`_SNIPPET`. Registrar o nome real no
> README.

- [ ] **Step 5: Bancada — checklist**

Gravar e verificar, anotando cada resposta no README:

1. `nrfutil device program --firmware .../build_lm20/*/zephyr/zephyr.hex --serial-number 1051898754 --options reset=RESET_SYSTEM`
2. **Em qual VCOM o prompt aparece?** (respondido no lab 6: a primeira, contra a segunda dos labs sem shield)
3. `wifi scan` lista as redes da sala
4. `wifi connect -s <ssid> -k 1 -p <senha>` conecta
5. `wifi status` mostra banda, canal, RSSI
6. `net iface` mostra o IP obtido por DHCP

- [ ] **Step 6: README**

`comms/06_wifi_shell/README.md`, abrindo com o aviso do console:

```markdown
# Wi-Fi · Lab 6 — O companion visível

> **Antes de tudo: o console mudou de porta.** Com a nRF7002-EB II acoplada, o overlay do
> shield desabilita a `uart20` e move o console para a `uart30` — ou seja, **outra VCOM**.
> Se você abrir a COM de sempre, não verá nada. Abra as duas e veja qual responde.

...
```

Seções: Hardware · Passo 1 compilar e gravar · Passo 2 achar o console · Passo 3 explorar
(`wifi scan`, `connect`, `status`, `net iface`) · O que observar · Pegadinhas · Fontes.

Incluir os números de FLASH/RAM medidos no Step 4 e a VCOM observada no Step 5.

- [ ] **Step 7: Commit**

```bash
git add comms/06_wifi_shell
git commit -m "06_wifi_shell: shell de Wi-Fi no LM20-DK + EB II, copia do SDK v3.4.0"
```

---

### Task 3: Lab 7 — `wifi/sta` com `minha_rede.conf`

**Files:**
- Create: `comms/07_wifi_sta/` (cópia de `nrf/samples/wifi/sta`), `minha_rede.conf`, `README.md`, `LICENSE`, `.gitignore`
- Modify: `comms/07_wifi_sta/CMakeLists.txt` (falha proposital), `comms/07_wifi_sta/prj.conf`

**Interfaces:**
- Consumes: convenções da Task 2.
- Produces: `minha_rede.conf` com `CONFIG_WIFI_CREDENTIALS_STATIC_SSID` e
  `CONFIG_WIFI_CREDENTIALS_STATIC_PASSWORD` — consumido pelos labs 9, 11, 12 e 13.

- [ ] **Step 1: Copiar e marcar**

```bash
mkdir -p comms/07_wifi_sta
cp -r /c/ncs/v3.4.0/nrf/samples/wifi/sta/* comms/07_wifi_sta/
rm -rf comms/07_wifi_sta/build*
cp comms/05_cs_iq_music/LICENSE comms/07_wifi_sta/LICENSE
```

Cabeçalho `ORIGEM:` em `prj.conf`, `CMakeLists.txt` e `src/main.c`, com
`DIVERGENCIA DO CURSO (duas): 1. minha_rede.conf; 2. falha de build no CMakeLists.`

- [ ] **Step 2: Criar `minha_rede.conf` — rastreado e vazio**

```
#
# Fragmento do aluno — a rede Wi-Fi da sala.
#
# Uso:
#   west build ... -- -DEXTRA_CONF_FILE=minha_rede.conf
#
# Deixar vazio faz o build falhar de proposito: um kit sem credencial nao
# conecta em lugar nenhum, e o erro em tempo de build e mais barato que
# descobrir isso com o log rodando.
#
# NUNCA commite a senha real. Antes de commitar:
#   git checkout comms/07_wifi_sta/minha_rede.conf
#
CONFIG_WIFI_CREDENTIALS_STATIC_SSID=""
CONFIG_WIFI_CREDENTIALS_STATIC_PASSWORD=""
```

- [ ] **Step 3: Falha proposital no `CMakeLists.txt`**

Depois de `project(...)`, acrescentar:

```cmake
# ALTERADO PELO CURSO (nrf-manaus-2): falha cedo e com mensagem util, em vez de
# gravar um kit que nao conecta em rede nenhuma.
if(NOT CONFIG_WIFI_CREDENTIALS_STATIC_SSID)
  message(FATAL_ERROR
    "CONFIG_WIFI_CREDENTIALS_STATIC_SSID nao definido.\n"
    "Preencha o minha_rede.conf com o SSID e a senha da rede da sala:\n"
    "  CONFIG_WIFI_CREDENTIALS_STATIC_SSID=\"nrf-curso\"\n"
    "  CONFIG_WIFI_CREDENTIALS_STATIC_PASSWORD=\"...\"\n"
    "e compile com -DEXTRA_CONF_FILE=minha_rede.conf")
endif()
```

- [ ] **Step 4: Remover as credenciais de exemplo do `prj.conf`**

Trocar as linhas `CONFIG_WIFI_CREDENTIALS_STATIC_SSID="Myssid"` e
`..._PASSWORD="Mypassword"` por um comentário marcado:

```
# ALTERADO PELO CURSO (nrf-manaus-2): as credenciais saem daqui e vao para o
# minha_rede.conf, um fragmento por aluno. Ver o CMakeLists.txt.
```

- [ ] **Step 5: Compilar com e sem o fragmento**

Sem o fragmento — deve **falhar** com a mensagem do Step 3:

```bash
cd /c/ncs/v3.4.0 && nrfutil sdk-manager toolchain launch --ncs-version v3.4.0 -- \
  west build -p -b nrf54lm20dk/nrf54lm20b/cpuapp --sysbuild \
  -d C:/work/nrf-manaus-2/comms/07_wifi_sta/build_falha \
  C:/work/nrf-manaus-2/comms/07_wifi_sta \
  -- -D07_wifi_sta_SHIELD="nrf7002eb2" -D07_wifi_sta_SNIPPET=nrf70-wifi
```

Esperado: `FATAL_ERROR` com o texto da mensagem. Apagar `build_falha`.

Com o fragmento preenchido temporariamente (SSID/senha reais) — deve compilar limpo. Depois:
`git checkout comms/07_wifi_sta/minha_rede.conf`.

- [ ] **Step 6: Bancada**

Gravar e conferir no console (`uart30`): `Connected`, IP por DHCP, e o que acontece ao
desligar o AP (reconexão). Anotar as linhas exatas do log para o README.

- [ ] **Step 7: README + commit**

README com: aviso do console, o `minha_rede.conf`, a falha proposital, os eventos de
`net_mgmt` que o aluno vê, e o teste de reconexão.

```bash
git add comms/07_wifi_sta
git commit -m "07_wifi_sta: associacao programatica com minha_rede.conf por aluno"
```

---

### Task 4: Lab 8 — provisionamento por SoftAP

**Files:**
- Create: `comms/08_wifi_provisioning/` (cópia de `nrf/samples/wifi/provisioning/softap`), `README.md`, `LICENSE`, `.gitignore`
- Create: `comms/08_wifi_provisioning/boards/nrf54lm20dk_nrf54lm20b_cpuapp.conf` **somente se** o Step 3 mostrar que falta

**Interfaces:**
- Consumes: convenções da Task 2.
- Produces: nada para os outros labs.

- [ ] **Step 1: Copiar**

```bash
mkdir -p comms/08_wifi_provisioning
cp -r /c/ncs/v3.4.0/nrf/samples/wifi/provisioning/softap/* comms/08_wifi_provisioning/
rm -rf comms/08_wifi_provisioning/build*
cp comms/05_cs_iq_music/LICENSE comms/08_wifi_provisioning/LICENSE
```

Cabeçalho `ORIGEM:` em `prj.conf`, `CMakeLists.txt` e `src/main.c`.
`DIVERGENCIA DO CURSO: nenhuma` (ou a do Step 3, se um `.conf` de board for criado).

- [ ] **Step 2: Verificar o `boards/`**

```bash
ls comms/08_wifi_provisioning/boards/
```

O sample traz `nrf54lm20dk_nrf54lm20a_cpuapp.conf` (variante **A**). Se não houver o da B, o
build do Step 3 dirá se ele é necessário.

- [ ] **Step 3: Compilar para a variante B**

```bash
cd /c/ncs/v3.4.0 && nrfutil sdk-manager toolchain launch --ncs-version v3.4.0 -- \
  west build -p -b nrf54lm20dk/nrf54lm20b/cpuapp --sysbuild \
  -d C:/work/nrf-manaus-2/comms/08_wifi_provisioning/build_lm20 \
  C:/work/nrf-manaus-2/comms/08_wifi_provisioning \
  -- -D08_wifi_provisioning_SHIELD="nrf7002eb2"
```

Se falhar por falta de config da variante B: copiar o `.conf` da variante A para
`nrf54lm20dk_nrf54lm20b_cpuapp.conf`, marcar com
`# ALTERADO PELO CURSO (nrf-manaus-2): copia do .conf da variante A; o sample so traz a A.`
e recompilar.

- [ ] **Step 4: Preparar o lado do PC**

```bash
cd comms/08_wifi_provisioning/scripts
protoc --proto_path=C:/ncs/v3.4.0/nrf/subsys/net/lib/softap_wifi_provision/proto \
       --python_out=. common.proto
```

Gera `common_pb2.py` — **não versionar** (entra no `.gitignore` do lab).

- [ ] **Step 5: Bancada — o fluxo completo**

1. gravar; no console, ver `Provisioning started` e o servidor DHCP subir
2. no notebook, conectar no Wi-Fi `nrf-wifiprov`
3. `python provision.py --certificate ../certs/server_certificate.pem`
4. conferir que o script **lista as redes que a DK viu** (SSID, RSSI, banda, canal, auth)
5. escolher a rede da sala e digitar a senha
6. confirmar no console que a DK associa

Anotar cada saída para o README.

- [ ] **Step 6: README + commit**

README com: aviso do console; o que o sample realmente é (HTTPS na 443 + protobuf, **não** um
formulário); o passo do `protoc`; o roteiro de seis passos acima; e a nota de que o momento
didático é a DK escanear por você.

Acrescentar ao `.gitignore` do lab:

```
# Gerado pelo protoc a partir do schema do SDK
scripts/common_pb2.py
```

```bash
git add comms/08_wifi_provisioning
git commit -m "08_wifi_provisioning: provisionamento por SoftAP com o provision.py da Nordic"
```

---

### Task 5: Lab 9 — payload JSON (código do curso, com testes)

Primeira metade do lab 9: a **única fonte do payload**, isolada para poder ser testada no PC
antes de existir firmware.

**Files:**
- Create: `comms/09_wifi_tcp/src/payload.h`, `comms/09_wifi_tcp/src/payload.c`
- Create: `comms/09_wifi_tcp/tools/tests/test_payload.py`, `comms/09_wifi_tcp/tools/payload_ref.py`

**Interfaces:**
- Consumes: nada.
- Produces:
  - C: `int payload_montar(char *buf, size_t buf_len, const struct payload_amostra *a);`
    com `struct payload_amostra { uint32_t seq; uint32_t uptime_ms; int16_t temp_cc; int8_t rssi_dbm; bool botao; };`
    (`temp_cc` = centésimos de grau) — devolve o número de bytes escritos ou negativo.
  - Python: `payload_ref.montar(seq, uptime_ms, temp_cc, rssi_dbm, botao) -> str` e
    `payload_ref.parse(linha) -> dict`, usados pelo servidor da Task 7 e pelos testes.

- [ ] **Step 1: Escrever o teste que falha**

`comms/09_wifi_tcp/tools/tests/test_payload.py`:

```python
# -*- coding: utf-8 -*-
import json
import pytest
from payload_ref import montar, parse


def test_monta_linha_json_com_todos_os_campos():
    linha = montar(seq=7, uptime_ms=1234, temp_cc=2537, rssi_dbm=-52, botao=False)
    assert linha.endswith("\n")
    d = json.loads(linha)
    assert d == {"seq": 7, "uptime_ms": 1234, "temp_c": 25.37,
                 "rssi_dbm": -52, "botao": False}


def test_temperatura_negativa_e_meio_grau():
    d = json.loads(montar(seq=1, uptime_ms=0, temp_cc=-450, rssi_dbm=-70, botao=True))
    assert d["temp_c"] == -4.5
    assert d["botao"] is True


def test_parse_e_o_inverso_de_montar():
    linha = montar(seq=99, uptime_ms=5, temp_cc=100, rssi_dbm=-1, botao=True)
    assert parse(linha) == {"seq": 99, "uptime_ms": 5, "temp_c": 1.0,
                            "rssi_dbm": -1, "botao": True}


def test_parse_rejeita_linha_invalida():
    assert parse("nao e json\n") is None
    assert parse('{"seq": 1}\n') is None          # faltam campos
```

- [ ] **Step 2: Rodar e ver falhar**

```bash
cd comms/09_wifi_tcp/tools && python -m pytest -q
```
Esperado: FAIL com `ModuleNotFoundError: No module named 'payload_ref'`.

- [ ] **Step 3: Implementar o `payload_ref.py`**

`comms/09_wifi_tcp/tools/payload_ref.py`:

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""Formato do payload do lab 9 — codigo do curso (nrf-manaus-2).

Uma linha JSON por amostra. E a MESMA estrutura que o firmware monta em
src/payload.c; este modulo existe para o servidor do PC e para os testes, e
para o aluno poder ler o formato sem abrir o C.

    {"seq":7,"uptime_ms":1234,"temp_c":25.37,"rssi_dbm":-52,"botao":false}

temp_c vem do firmware em centesimos de grau (int) para nao precisar de float
na serializacao embarcada; aqui vira grau com duas casas.
"""
from __future__ import annotations

import json

CAMPOS = ("seq", "uptime_ms", "temp_c", "rssi_dbm", "botao")


def montar(seq: int, uptime_ms: int, temp_cc: int, rssi_dbm: int, botao: bool) -> str:
    return json.dumps({
        "seq": seq,
        "uptime_ms": uptime_ms,
        "temp_c": round(temp_cc / 100.0, 2),
        "rssi_dbm": rssi_dbm,
        "botao": botao,
    }, separators=(",", ":")) + "\n"


def parse(linha: str):
    """Devolve o dicionario da amostra, ou None se a linha nao for uma."""
    try:
        d = json.loads(linha)
    except (ValueError, TypeError):
        return None
    if not isinstance(d, dict) or any(c not in d for c in CAMPOS):
        return None
    return {c: d[c] for c in CAMPOS}
```

- [ ] **Step 4: Rodar e ver passar**

```bash
cd comms/09_wifi_tcp/tools && python -m pytest -q
```
Esperado: `4 passed`.

- [ ] **Step 5: Escrever o `payload.h` e o `payload.c` do firmware**

`comms/09_wifi_tcp/src/payload.h`:

```c
/*
 * Codigo do curso (nrf-manaus-2) — nao vem do SDK.
 *
 * Monta a linha JSON que os labs 9, 10 e 13 mandam pelo transporte escolhido.
 * O formato esta espelhado em tools/payload_ref.py, que os testes cobrem.
 */
#ifndef LAB_PAYLOAD_H_
#define LAB_PAYLOAD_H_

#include <stdbool.h>
#include <stddef.h>
#include <stdint.h>

struct payload_amostra {
	uint32_t seq;
	uint32_t uptime_ms;
	int16_t temp_cc;     /* centesimos de grau: 2537 = 25,37 C */
	int8_t rssi_dbm;
	bool botao;
};

/* Escreve a linha JSON (terminada em \n) em buf. Devolve os bytes escritos,
 * ou -ENOMEM se nao couber.
 */
int payload_montar(char *buf, size_t buf_len, const struct payload_amostra *a);

#endif /* LAB_PAYLOAD_H_ */
```

`comms/09_wifi_tcp/src/payload.c`:

```c
/* Codigo do curso (nrf-manaus-2). Espelhado em tools/payload_ref.py. */
#include <errno.h>
#include <stdio.h>

#include "payload.h"

int payload_montar(char *buf, size_t buf_len, const struct payload_amostra *a)
{
	int n = snprintf(buf, buf_len,
			 "{\"seq\":%u,\"uptime_ms\":%u,\"temp_c\":%d.%02d,"
			 "\"rssi_dbm\":%d,\"botao\":%s}\n",
			 a->seq, a->uptime_ms,
			 a->temp_cc / 100, (a->temp_cc < 0 ? -a->temp_cc : a->temp_cc) % 100,
			 a->rssi_dbm, a->botao ? "true" : "false");

	if (n < 0 || (size_t)n >= buf_len) {
		return -ENOMEM;
	}
	return n;
}
```

> Cuidado com temperatura negativa entre -1 e 0 °C: `-50` daria `"0.50"` em vez de `"-0.50"`.
> A bancada do curso não chega perto disso, e o `payload_ref.py` (que é o que o servidor usa
> para interpretar) não tem o problema. Registrar essa limitação num comentário no `.c`.

- [ ] **Step 6: Commit**

```bash
git add comms/09_wifi_tcp/src/payload.h comms/09_wifi_tcp/src/payload.c \
        comms/09_wifi_tcp/tools/payload_ref.py comms/09_wifi_tcp/tools/tests/test_payload.py
git commit -m "09_wifi_tcp: payload JSON de uma linha, em C e em Python, com testes"
```

---

### Task 6: Lab 9 — firmware: conexão, telemetria, botão e LED

**Files:**
- Create: `comms/09_wifi_tcp/src/main.c`, `src/transporte.h`, `src/transporte.c`
- Create: `comms/09_wifi_tcp/CMakeLists.txt`, `Kconfig`, `prj.conf`, `minha_rede.conf`, `.gitignore`, `LICENSE`

**Interfaces:**
- Consumes: `payload_montar()` e `struct payload_amostra` (Task 5); `minha_rede.conf` (Task 3).
- Produces:
  - `int transporte_abrir(void);` `int transporte_enviar(const char *buf, size_t len);`
    `int transporte_receber(char *buf, size_t len, k_timeout_t espera);` `void transporte_fechar(void);`
    — a Task 8 acrescenta implementações de HTTP e MQTT atrás desta mesma interface.
  - Kconfig: `LAB_SERVIDOR_IP` (string), `LAB_PORTA` (int, default 9000),
    `LAB_INTERVALO_MS` (int, default 2000), `LAB_TRANSPORTE` (choice: `LAB_TRANSPORTE_TCP` default).

- [ ] **Step 1: `Kconfig` do lab**

```
#
# Codigo do curso (nrf-manaus-2).
#
menu "Lab: telemetria por socket (nrf-manaus-2)"

config LAB_SERVIDOR_IP
	string "IP do servidor no PC"
	default ""
	help
	  IPv4 do PC que roda o tools/wifi_server.py. Vazio faz o build falhar.

config LAB_PORTA
	int "Porta TCP do servidor"
	default 9000

config LAB_INTERVALO_MS
	int "Intervalo entre amostras de telemetria (ms)"
	default 2000
	range 200 60000

choice LAB_TRANSPORTE
	prompt "Transporte"
	default LAB_TRANSPORTE_TCP
	help
	  O mesmo payload sai pelos tres. Ver o lab 10 para a comparacao.

config LAB_TRANSPORTE_TCP
	bool "TCP puro"

endchoice

endmenu

source "Kconfig.zephyr"
```

> As opções `LAB_TRANSPORTE_HTTP` e `LAB_TRANSPORTE_MQTT` entram na Task 8.

- [ ] **Step 2: `transporte.h` e o TCP**

`src/transporte.h`:

```c
/* Codigo do curso (nrf-manaus-2).
 *
 * Uma interface, tres implementacoes (TCP, HTTP, MQTT). O main.c nao sabe qual
 * esta compilada: e a tese do bloco — mesmo payload, transporte trocado.
 */
#ifndef LAB_TRANSPORTE_H_
#define LAB_TRANSPORTE_H_

#include <stddef.h>
#include <zephyr/kernel.h>

int transporte_abrir(void);
int transporte_enviar(const char *buf, size_t len);
/* Devolve bytes lidos, 0 se nada chegou dentro de `espera`, ou negativo em erro. */
int transporte_receber(char *buf, size_t len, k_timeout_t espera);
void transporte_fechar(void);

#endif /* LAB_TRANSPORTE_H_ */
```

`src/transporte.c` implementa a variante TCP sob `#if defined(CONFIG_LAB_TRANSPORTE_TCP)`:
socket `AF_INET`/`SOCK_STREAM`, `connect()` no `CONFIG_LAB_SERVIDOR_IP:CONFIG_LAB_PORTA`,
`send()` direto, e `recv()` com `zsock_poll()` para o timeout.

- [ ] **Step 3: `main.c`**

Estrutura, com o esqueleto de conexão do `wifi/sta`:

1. `net_mgmt` callbacks para `NET_EVENT_WIFI_CONNECT_RESULT` e `NET_EVENT_IPV4_DHCP_BOUND`
2. espera o IP, chama `transporte_abrir()`
3. thread de telemetria: a cada `CONFIG_LAB_INTERVALO_MS`, lê temperatura
   (`DEVICE_DT_GET(DT_NODELABEL(temp_sensor))`, `sensor_sample_fetch` +
   `sensor_channel_get(SENSOR_CHAN_DIE_TEMP)`), lê RSSI (`net_mgmt(NET_REQUEST_WIFI_IFACE_STATUS)`),
   monta com `payload_montar()` e envia
4. `gpio` callback no `sw0` (`DT_ALIAS(sw0)`): manda uma amostra imediata com `botao=true`
5. thread de recepção: `transporte_receber()`; se a linha for `LED 1` acende `led1`
   (`DT_ALIAS(led1)`), `LED 0` apaga

- [ ] **Step 4: `CMakeLists.txt` com falha proposital**

```cmake
cmake_minimum_required(VERSION 3.20.0)
find_package(Zephyr REQUIRED HINTS $ENV{ZEPHYR_BASE})
project(lab_wifi_tcp)

# Codigo do curso (nrf-manaus-2): falha cedo em vez de gravar um kit que nao
# sabe para onde mandar os dados.
if(NOT CONFIG_LAB_SERVIDOR_IP)
  message(FATAL_ERROR
    "CONFIG_LAB_SERVIDOR_IP nao definido.\n"
    "Descubra o IP do seu PC na rede da sala (ipconfig) e passe:\n"
    "  -DCONFIG_LAB_SERVIDOR_IP=\"192.168.0.100\"")
endif()

target_sources(app PRIVATE src/main.c src/payload.c src/transporte.c)
```

- [ ] **Step 5: `prj.conf`**

Partir do `prj.conf` do `wifi/sta` (blocos de `CONFIG_WIFI*`, `CONFIG_NET*`, DHCP), tirar as
credenciais de exemplo e acrescentar:

```
# Codigo do curso (nrf-manaus-2)
CONFIG_GPIO=y
CONFIG_SENSOR=y
CONFIG_TEMP_NRF5=y
CONFIG_CBPRINTF_FP_SUPPORT=n
CONFIG_MAIN_STACK_SIZE=4096
```

- [ ] **Step 6: Compilar**

```bash
cd /c/ncs/v3.4.0 && nrfutil sdk-manager toolchain launch --ncs-version v3.4.0 -- \
  west build -p -b nrf54lm20dk/nrf54lm20b/cpuapp --sysbuild \
  -d C:/work/nrf-manaus-2/comms/09_wifi_tcp/build_lm20 \
  C:/work/nrf-manaus-2/comms/09_wifi_tcp \
  -- -D09_wifi_tcp_SHIELD="nrf7002eb2" -D09_wifi_tcp_SNIPPET=nrf70-wifi \
     -DEXTRA_CONF_FILE=minha_rede.conf -DCONFIG_LAB_SERVIDOR_IP=\"192.168.0.100\"
```

Esperado: `exit 0`. Anotar FLASH/RAM — é a linha "TCP" da tabela do lab 10.

- [ ] **Step 7: Commit**

```bash
git add comms/09_wifi_tcp
git commit -m "09_wifi_tcp: firmware de telemetria, botao e LED sobre socket TCP"
```

---

### Task 7: Lab 9 — `wifi_server.py` e o fechamento do lab

**Files:**
- Create: `comms/09_wifi_tcp/tools/wifi_server.py`, `tools/requirements.txt`, `tools/tests/test_server.py`, `tools/tests/conftest.py`
- Create: `comms/09_wifi_tcp/README.md`

**Interfaces:**
- Consumes: `payload_ref.parse()` (Task 5); o firmware da Task 6.
- Produces: `wifi_server.py` — reusado como referência pelo lab 13.

- [ ] **Step 1: Teste que falha**

`tools/tests/test_server.py`:

```python
# -*- coding: utf-8 -*-
import socket
import threading
import time

from wifi_server import Servidor


def test_recebe_amostras_e_devolve_comando_de_led():
    recebidas = []
    srv = Servidor(porta=0, ao_receber=recebidas.append)
    t = threading.Thread(target=srv.servir_uma_conexao, daemon=True)
    t.start()
    time.sleep(0.1)

    with socket.create_connection(("127.0.0.1", srv.porta_real), timeout=2) as s:
        s.sendall(b'{"seq":1,"uptime_ms":10,"temp_c":25.0,"rssi_dbm":-40,"botao":false}\n')
        s.sendall(b'{"seq":2,"uptime_ms":20,"temp_c":25.1,"rssi_dbm":-41,"botao":true}\n')
        time.sleep(0.2)
        srv.enviar_comando("LED 1")
        assert s.recv(16) == b"LED 1\n"

    t.join(timeout=2)
    assert [a["seq"] for a in recebidas] == [1, 2]
    assert recebidas[1]["botao"] is True


def test_ignora_lixo_na_serial_sem_derrubar_a_conexao():
    recebidas = []
    srv = Servidor(porta=0, ao_receber=recebidas.append)
    t = threading.Thread(target=srv.servir_uma_conexao, daemon=True)
    t.start()
    time.sleep(0.1)
    with socket.create_connection(("127.0.0.1", srv.porta_real), timeout=2) as s:
        s.sendall(b"lixo\n")
        s.sendall(b'{"seq":9,"uptime_ms":1,"temp_c":1.0,"rssi_dbm":-1,"botao":false}\n')
        time.sleep(0.2)
    t.join(timeout=2)
    assert [a["seq"] for a in recebidas] == [9]
```

`tools/tests/conftest.py` acrescenta o diretório pai ao `sys.path`, igual ao do lab 5.

- [ ] **Step 2: Rodar e ver falhar**

```bash
cd comms/09_wifi_tcp/tools && python -m pytest -q
```
Esperado: FAIL — `No module named 'wifi_server'`.

- [ ] **Step 3: Implementar o `wifi_server.py`**

Classe `Servidor(porta, ao_receber)` com:
- `porta_real` (resolve `porta=0` para a porta efetiva, o que torna o teste determinístico)
- `servir_uma_conexao()`: aceita um cliente, lê linhas, chama `payload_ref.parse()`, ignora o
  que não for amostra, chama `ao_receber(dict)`
- `enviar_comando(txt)`: manda `txt + "\n"` para o cliente conectado
- `main()`: CLI com `--porta 9000`, imprime cada amostra numa linha alinhada e lê o teclado
  (`l` liga o LED, `d` desliga, `q` sai)

- [ ] **Step 4: Rodar e ver passar**

```bash
cd comms/09_wifi_tcp/tools && python -m pytest -q
```
Esperado: `6 passed` (4 do payload + 2 do servidor).

- [ ] **Step 5: Bancada — o lab inteiro**

1. `python wifi_server.py --porta 9000` no PC
2. gravar a DK com o IP do PC
3. conferir: amostras chegando a cada 2 s, `seq` incrementando
4. apertar o **botão 1** (`sw0`) → amostra imediata com `"botao":true`
5. teclar `l` no servidor → **LED 2** acende; `d` → apaga
6. andar com o kit até cair e voltar → **buraco no `seq`**, que é o ponto do lab
7. encostar o dedo no chip → `temp_c` sobe

- [ ] **Step 6: README + commit**

README com: aviso do console, o payload comentado campo a campo, os três gestos, o roteiro de
bancada acima, o plano B em SoftAP, e a tabela de FLASH/RAM.

```bash
git add comms/09_wifi_tcp
git commit -m "09_wifi_tcp: servidor Python, testes e roteiro de bancada"
```

---

### Task 8: Lab 10 — HTTP e MQTT sobre o mesmo payload

**Files:**
- Modify: `comms/09_wifi_tcp/Kconfig` (mais duas opções na choice), `src/transporte.c` (mais duas implementações), `prj.conf`
- Create: `comms/10_wifi_http_mqtt/README.md`, `tools/wifi_http_server.py`, `tools/wifi_mqtt_sub.py`, `tools/requirements.txt`

**Interfaces:**
- Consumes: a interface `transporte_*` da Task 6.
- Produces: a tabela comparativa (bytes, flash, o que o outro lado exige) que vai ao deck.

- [ ] **Step 1: Acrescentar as opções ao `Kconfig` do lab 9**

```
config LAB_TRANSPORTE_HTTP
	bool "HTTP (POST por amostra)"

config LAB_TRANSPORTE_MQTT
	bool "MQTT (publish/subscribe)"
```

E, fora da choice:

```
config LAB_MQTT_TOPICO
	string "Topico MQTT"
	default "nrf-manaus/telemetria"
	depends on LAB_TRANSPORTE_MQTT
```

- [ ] **Step 2: HTTP em `transporte.c`**

Sob `#elif defined(CONFIG_LAB_TRANSPORTE_HTTP)`: usar `http_client_req()` do Zephyr,
`POST /telemetria`, `Content-Type: application/json`, corpo = a linha do payload.
`transporte_receber()` faz `GET /comando` — e o README explica que **é polling, e é esse o
incômodo do HTTP**.

Acrescentar ao `prj.conf`: `CONFIG_HTTP_CLIENT=y`.

- [ ] **Step 3: MQTT em `transporte.c`**

Sob `#elif defined(CONFIG_LAB_TRANSPORTE_MQTT)`: cliente MQTT do Zephyr, `publish` no tópico
`CONFIG_LAB_MQTT_TOPICO`, `subscribe` em `<topico>/comando`, e `transporte_receber()` devolve
o que chegou por callback.

Acrescentar ao `prj.conf`: `CONFIG_MQTT_LIB=y`, `CONFIG_MQTT_CLEAN_SESSION=y`.

- [ ] **Step 4: Compilar os três e anotar**

```bash
for T in TCP HTTP MQTT; do
  cd /c/ncs/v3.4.0 && nrfutil sdk-manager toolchain launch --ncs-version v3.4.0 -- \
    west build -p -b nrf54lm20dk/nrf54lm20b/cpuapp --sysbuild \
    -d C:/work/nrf-manaus-2/comms/09_wifi_tcp/build_$T \
    C:/work/nrf-manaus-2/comms/09_wifi_tcp \
    -- -D09_wifi_tcp_SHIELD="nrf7002eb2" -D09_wifi_tcp_SNIPPET=nrf70-wifi \
       -DEXTRA_CONF_FILE=minha_rede.conf \
       -DCONFIG_LAB_SERVIDOR_IP=\"192.168.0.100\" -DCONFIG_LAB_TRANSPORTE_$T=y
done
```

Anotar FLASH e RAM dos três. **Se o MQTT não couber na RAM**, registrar o número e marcar o
MQTT como referência lida no README, conforme §4.5 da spec.

- [ ] **Step 5: Ferramentas do PC**

- `wifi_http_server.py`: `http.server` mínimo, `POST /telemetria` imprime a amostra,
  `GET /comando` devolve o comando pendente (ou 204)
- `wifi_mqtt_sub.py`: `paho-mqtt`, assina o tópico, imprime, e publica o comando do teclado

`requirements.txt` do lab 10: `paho-mqtt>=2.0`.

- [ ] **Step 6: Bancada dos três + medir bytes no fio**

Com o Wireshark ou `tcpdump` no PC, medir os **bytes por amostra** de cada transporte.
Preencher a tabela do README.

- [ ] **Step 7: README + commit**

README do lab 10 com: o que muda (nada no payload), a tabela medida (bytes/amostra, FLASH,
RAM, o que o outro lado precisa), e **por que o downlink separa os três** — o `GET /comando`
do HTTP é o argumento.

```bash
git add comms/09_wifi_tcp comms/10_wifi_http_mqtt
git commit -m "10_wifi_http_mqtt: mesmo payload por HTTP e MQTT, com a comparacao medida"
```

---

### Task 9: Lab 11 — a escada de economia de energia, medida no PPK2

**Parcialmente bloqueada.** A tarefa tem duas partes, e só a segunda depende do EX3000:

- **Parte A — degraus 1 e 2 (DTIM e listen interval).** Não precisa de AP com TWT nem de
  Wi-Fi 6. Roda em qualquer rede, inclusive a ONT da bancada. **Pode ser feita agora.**
- **Parte B — degrau 3 (TWT).** Depende da Task 1 confirmar que o EX3000 negocia TWT
  individual. Medido: a ONT da bancada é 802.11ax e responde `Peer not TWT capable`.

Se o EX3000 falhar também, a Parte A sozinha sustenta o lab e o slide de TWT vira conceito mais
o resultado negativo, que tem valor didático próprio (§6.1 e §7.1 da spec).

**Leia o §7.1 e o §7.2 da spec antes de escrever qualquer texto deste lab.** Eles definem a
escada de três degraus, a relação entre TIM e DTIM, e as três armadilhas do material online da
Nordic que não transferem para a nossa bancada.

**Files:**
- Create: `comms/11_wifi_twt/` (cópia de `nrf/samples/wifi/twt`), `minha_rede.conf`, `README.md`, `LICENSE`, `.gitignore`

**Interfaces:**
- Consumes: `minha_rede.conf` (Task 3); resultado da Task 1 (só para a Parte B).
- Produces: as correntes médias dos três (ou dois) regimes, para o deck.

- [ ] **Step 1: Copiar e marcar**

```bash
mkdir -p comms/11_wifi_twt
cp -r /c/ncs/v3.4.0/nrf/samples/wifi/twt/* comms/11_wifi_twt/
rm -rf comms/11_wifi_twt/build*
cp comms/05_cs_iq_music/LICENSE comms/11_wifi_twt/LICENSE
```

Cabeçalho `ORIGEM:`; divergência = `minha_rede.conf`.

- [ ] **Step 2: Servidor de tráfego no PC**

```bash
python /c/ncs/v3.4.0/nrf/scripts/traffic_gen_server.py
```

Ajustar `CONFIG_TRAFFIC_GEN_REMOTE_IPV4_ADDR` para o IP do PC.

- [ ] **Step 3: Compilar e gravar**

```bash
cd /c/ncs/v3.4.0 && nrfutil sdk-manager toolchain launch --ncs-version v3.4.0 -- \
  west build -p -b nrf54lm20dk/nrf54lm20b/cpuapp --sysbuild \
  -d C:/work/nrf-manaus-2/comms/11_wifi_twt/build_lm20 \
  C:/work/nrf-manaus-2/comms/11_wifi_twt \
  -- -D11_wifi_twt_SHIELD="nrf7002eb2" -D11_wifi_twt_SNIPPET=nrf70-wifi \
     -DEXTRA_CONF_FILE=minha_rede.conf
```

- [ ] **Step 4: Achar o ponto de medição do PPK2 nesta bancada**

**Item em aberto, resolver antes de medir.** As instruções publicadas pela Nordic (remover o
jumper P23 de VBAT, Vout no P23 pino 1, GND no P21) são do **nRF7002 DK**, host nRF5340. A nossa
bancada é **nRF54LM20-DK + nRF7002-EB II** e o ponto é outro: a EB II tem cabeçalho próprio de
medição de corrente. Levantar na documentação de hardware da EB II e do LM20-DK e **registrar no
README qual ponto foi usado e o que ele mede** — só o companion, ou companion mais host. A
interpretação de todo número depende disso.

PPK2 em modo *source meter*, 3,6 V.

> **Atenção:** foi o PPK2 no jumper de corrente que quebrou o SWD nos labs de Channel Sounding.
> Gravar a DK **antes** de colocar o PPK2, e recolocar o jumper ao terminar.

- [ ] **Step 5: Medir os regimes — Parte A, sem depender do EX3000**

Com o firmware do **lab 6** (o shell), que já expõe todos os comandos necessários. 60 s por
regime, corrente média:

1. **Sem economia:** `wifi ps off`
2. **DTIM (padrão):** `wifi ps on`, `wifi ps_wakeup_mode dtim`
3. **Listen interval:** `wifi listen_interval <n>` **antes de conectar** (é um campo do quadro de
   associação), depois `wifi ps_wakeup_mode listen_interval`

Antes de cada medida, rodar `wifi ps` e `wifi status` e anotar o que a saída diz: modo, listen
interval, beacon interval e DTIM period da rede. Sem esses quatro números a corrente não se
interpreta. Medido na bancada de preparação: beacon interval 100, DTIM 3, listen interval 10.

Lembrar que trocar o modo de despertar em tempo de execução funciona nos dois sentidos, mas
**mudar o valor do listen interval exige reconectar**.

- [ ] **Step 6: Medir o TWT — Parte B, depende do EX3000**

Só depois que a Task 1 confirmar a negociação. Estabelecer o fluxo e medir 60 s:

```
wifi twt quick_setup <wake_interval_us> <interval_us>
wifi twt teardown_all
```

> **Armadilha do material online:** o curso e o blog da Nordic usam a forma posicional
> (`wifi twt setup 0 0 1 1 0 1 1 1 8 60000`). Testado nesta árvore: **falha** com
> `setup: wrong parameter count`. Nesta versão o comando é por opções longas, com 25 argumentos,
> ou o atalho `quick_setup`. Registrar isso no README — o aluno vai copiar do material online.

Se o EX3000 não negociar, registrar a saída de recusa e seguir só com a Parte A.

- [ ] **Step 7: README + commit**

README com: a escada de três degraus resumida do §7.1 da spec; qual ponto de medição foi usado e
o que ele mede; o roteiro do PPK2 com o aviso do jumper; a tabela das correntes medidas, uma
linha por regime, com as linhas de TWT marcadas como pendentes se a Parte B não rodou; e as três
armadilhas do material online do §7.2.

**Nenhum número da Nordic entra na tabela do curso.** As correntes publicadas por eles são de
nRF7002 DK com host nRF5340. Podem ser citadas no texto como ordem de grandeza, sempre
atribuídas.

```bash
git add comms/11_wifi_twt
git commit -m "11_wifi_twt: os regimes de economia medidos no PPK2"
```

---

### Task 10: Lab 12 — coexistência BLE + Wi-Fi

**Files:**
- Create: `comms/12_wifi_coex/` (cópia de `nrf/samples/wifi/ble_coex`), `minha_rede.conf`, `README.md`, `LICENSE`, `.gitignore`

**Interfaces:**
- Consumes: `minha_rede.conf` (Task 3).
- Produces: tabela de throughput Wi-Fi × BLE, com e sem coexistência.

- [ ] **Step 1: Copiar e marcar**

```bash
mkdir -p comms/12_wifi_coex
cp -r /c/ncs/v3.4.0/nrf/samples/wifi/ble_coex/* comms/12_wifi_coex/
rm -rf comms/12_wifi_coex/build*
cp comms/05_cs_iq_music/LICENSE comms/12_wifi_coex/LICENSE
```

- [ ] **Step 2: Compilar com o shield duplo**

```bash
cd /c/ncs/v3.4.0 && nrfutil sdk-manager toolchain launch --ncs-version v3.4.0 -- \
  west build -p -b nrf54lm20dk/nrf54lm20b/cpuapp --sysbuild \
  -d C:/work/nrf-manaus-2/comms/12_wifi_coex/build_lm20 \
  C:/work/nrf-manaus-2/comms/12_wifi_coex \
  -- -D12_wifi_coex_SHIELD="nrf7002eb2;nrf7002eb2_coex" \
     -D12_wifi_coex_SNIPPET=nrf70-wifi -DEXTRA_CONF_FILE=minha_rede.conf
```

Se o `sample.yaml` do SDK usar outro prefixo de imagem, ajustar e registrar no README.

- [ ] **Step 3: Bancada — as duas medidas**

O sample precisa de um par BLE. Usar o nRF54L15-TAG do Channel Sounding, ou uma segunda DK.

Rodar com coexistência **ligada** e **desligada** (conforme o Kconfig do sample), anotando
throughput de Wi-Fi e de BLE em cada caso.

- [ ] **Step 4: README + commit**

README amarrando com o Channel Sounding: a turma mediu distância por BLE em 2,4 GHz; aqui vê o
que o Wi-Fi faz com o mesmo espectro e como o árbitro divide o meio. Tabela com as quatro
medidas.

```bash
git add comms/12_wifi_coex
git commit -m "12_wifi_coex: throughput de Wi-Fi e BLE juntos, com e sem coexistencia"
```

---

### Task 11: Lab 13 — locationing por scan

**Files:**
- Create: `comms/13_wifi_location/src/main.c`, `CMakeLists.txt`, `Kconfig`, `prj.conf`, `minha_rede.conf`, `LICENSE`, `.gitignore`
- Create: `comms/13_wifi_location/tools/wifi_locate.py`, `tools/requirements.txt`, `tools/tests/test_locate.py`, `tools/tests/conftest.py`
- Create: `comms/13_wifi_location/README.md`

**Interfaces:**
- Consumes: o padrão de socket TCP da Task 6 (reimplementado aqui, ~40 linhas — apps são
  freestanding por convenção do módulo).
- Produces: nada para os outros labs.

- [ ] **Step 1: Teste que falha, no lado do PC**

`tools/tests/test_locate.py`:

```python
# -*- coding: utf-8 -*-
import json
import pytest
from wifi_locate import montar_requisicao, parse_scan


def test_parse_scan_le_a_linha_do_firmware():
    linha = 'AP,d8:ec:5e:8f:66:19,-49,5180,cia-linksys\n'
    assert parse_scan(linha) == {"macAddress": "d8:ec:5e:8f:66:19",
                                 "signalStrength": -49,
                                 "frequency": 5180,
                                 "ssid": "cia-linksys"}


def test_parse_scan_rejeita_outras_linhas():
    assert parse_scan("qualquer coisa\n") is None
    assert parse_scan("AP,so,dois\n") is None


def test_requisicao_tem_o_formato_do_nrf_cloud():
    aps = [{"macAddress": "d8:ec:5e:8f:66:19", "signalStrength": -49,
            "frequency": 5180, "ssid": "a"},
           {"macAddress": "c8:7f:54:de:23:b8", "signalStrength": -60,
            "frequency": 2412, "ssid": "b"}]
    corpo = montar_requisicao(aps)
    assert list(corpo.keys()) == ["accessPoints"]
    assert corpo["accessPoints"][0]["macAddress"] == "d8:ec:5e:8f:66:19"
    assert corpo["accessPoints"][0]["signalStrength"] == -49


def test_requisicao_exige_pelo_menos_dois_aps():
    with pytest.raises(ValueError, match="dois"):
        montar_requisicao([{"macAddress": "d8:ec:5e:8f:66:19", "signalStrength": -49}])
```

- [ ] **Step 2: Rodar e ver falhar**

```bash
cd comms/13_wifi_location/tools && python -m pytest -q
```
Esperado: FAIL — `No module named 'wifi_locate'`.

- [ ] **Step 3: Implementar o `wifi_locate.py`**

Funções puras (testáveis) mais o CLI:

```python
ENDPOINT = ("https://api.nrfcloud.com/v1/organizations/{org}/projects/{proj}"
            "/location/wifi")


def parse_scan(linha: str):
    """AP,<bssid>,<rssi>,<freq>,<ssid> -> dict do formato do nRF Cloud."""


def montar_requisicao(aps: list) -> dict:
    """{"accessPoints": [...]}; levanta ValueError se houver menos de dois."""


def resolver(aps, org, proj, token):
    """POST no ENDPOINT com Authorization: Bearer <token>.
    Devolve (lat, lon, uncertainty)."""
```

CLI: `--porta 9000` (recebe o scan da DK por TCP), `--org`, `--proj`, e o token em
`NRFCLOUD_OAT`. Imprime a lista de APs recebida **e** a coordenada — as duas metades.

`requirements.txt`: `requests>=2.31`.

- [ ] **Step 4: Rodar e ver passar**

```bash
cd comms/13_wifi_location/tools && python -m pytest -q
```
Esperado: `4 passed`.

- [ ] **Step 5: Firmware**

`src/main.c`: conecta (esqueleto do `sta`), faz `NET_REQUEST_WIFI_SCAN`, acumula os resultados
no callback (`bssid`, `rssi`, `frequency`, `ssid`), e ao terminar abre o socket e manda uma
linha `AP,<bssid>,<rssi>,<freq>,<ssid>` por AP, seguida de `FIM\n`. Repete ao apertar `sw0`.

`Kconfig` com `LAB_SERVIDOR_IP` e `LAB_PORTA` (mesmos nomes do lab 9) e a mesma falha
proposital no `CMakeLists.txt`.

- [ ] **Step 6: Compilar e bancada**

Compilar como nas tarefas anteriores. Na bancada:

1. `set NRFCLOUD_OAT=...` e `python wifi_locate.py --org <slug> --proj <slug>`
2. gravar a DK
3. conferir: a lista de APs que o kit viu, e depois `lat`, `lon`, `uncertainty`
4. comparar com a posição real da sala e anotar o erro

Se a resposta for erro de MAC local, ver a nota da Nordic sobre endereços localmente
administrados e descartar esses APs.

- [ ] **Step 7: README + commit**

README com: o princípio (a localização está no banco de dados de quem mapeou os APs, não no
dispositivo), o fluxo, o que precisa (chave OAT, internet **no PC**, dois APs no mínimo), o
erro medido, e a referência ao `wifi/nrf_cloud` como o caminho com TF-M e onboarding.

```bash
git add comms/13_wifi_location
git commit -m "13_wifi_location: scan no kit, posicao resolvida no PC pelo nRF Cloud"
```

---

### Task 12: Índice do módulo, pré-requisitos e cronometragem

**Files:**
- Modify: `comms/README.md`, `PREREQUISITOS.md`, `README.md` (raiz)
- Create: `comms/TEMPOS_WIFI.md`

**Interfaces:**
- Consumes: os oito labs.
- Produces: o número que decide o corte de escopo (§1 da spec).

- [ ] **Step 1: Índice de `comms/README.md`**

Acrescentar as oito linhas na tabela de labs, no formato das cinco de CS, e a seção
"Wi-Fi — ordem de ensino" no estilo do bloco de CS.

- [ ] **Step 2: `PREREQUISITOS.md`**

Acrescentar ao módulo `comms/`: `protoc` + pacote `protobuf` (lab 8), `paho-mqtt` e `requests`
(labs 10 e 13), chave OAT do nRF Cloud e slugs de organização/projeto (lab 13), PPK2 (lab 11),
e o TP-Link EX3000 como AP de referência.

- [ ] **Step 3: Cronometrar os oito**

Rodar cada lab do zero, como um aluno faria, e cronometrar. Registrar em
`comms/TEMPOS_WIFI.md`: lab, tempo medido, e o que domina o tempo (build? bancada? setup do
PC?).

- [ ] **Step 4: Recomendação de corte**

No mesmo arquivo, uma seção "Se só couberem 3h" com a sugestão de quais labs ficam e quais
viram demo, **com o tempo medido como argumento**. A decisão é do instrutor.

- [ ] **Step 5: Commit**

```bash
git add comms/README.md PREREQUISITOS.md README.md comms/TEMPOS_WIFI.md
git commit -m "comms: indice dos oito labs de Wi-Fi, pre-requisitos e tempos medidos"
```

---

### Task 13: Deck M2-04

**Files:**
- Create: `doc/_template/build_m2_04.py`, `doc/_template/fig_m2_04_companion.py`, `doc/_template/fig_m2_04_transportes.py`
- Create (gerados): `doc/comms/M2-04_WiFi.pptx`, `doc/comms/img/wifi_*.png`

**Interfaces:**
- Consumes: os números medidos nas Tasks 6–11.
- Produces: o deck.

- [ ] **Step 1: Figura do companion**

`fig_m2_04_companion.py`, no molde do `fig_m2_03_antenas_hw.py`: LM20-DK e nRF7002 lado a lado,
o SPI (P3.00–P3.03), BUCKEN/IOVDDEN, IRQ, o blob de firmware subindo para o nRF7002, e a nota
de que o microfone PDM disputa P1.04/P1.05.

- [ ] **Step 2: Figura dos transportes**

`fig_m2_04_transportes.py`: barras com bytes por amostra, FLASH e RAM dos três transportes, com
os números medidos na Task 8.

- [ ] **Step 2b: Figura do beacon, TIM e DTIM**

`fig_m2_04_dtim.py`: uma linha do tempo de beacons igualmente espaçados no beacon interval, com
o elemento TIM marcado em todos e o DTIM destacado a cada 3 (o valor medido na bancada). Abaixo
dela, duas faixas de atividade da estação sobrepostas ao mesmo eixo: uma acordando em todo
DTIM, outra dormindo através de vários e acordando no horário negociado do TWT. Sem números
inventados: o beacon interval e o período de DTIM saem da bancada.

- [ ] **Step 3: `build_m2_04.py`**

No molde dos `build_m2_0{1,2,3}.py`. Slides: capa; o que é um companion IC; os blobs; a
armadilha do console; provisionamento (SoftAP × BLE); o dispositivo IoT dos três gestos; a
tabela dos três transportes; **o bloco de economia de energia (ver abaixo)**; coexistência
amarrando com o CS; locationing e o complemento ao GNSS; takeaway.

**O bloco de economia de energia são quatro slides, não um**, e o conteúdo exato está no §7.1
da spec — leia lá antes de escrever. A sequência obrigatória:

1. **Beacon, TIM e DTIM.** Apoiado na `fig_m2_04_dtim.py` (Step 2b). O ponto a fixar: todo
   beacon carrega um TIM; um beacon a cada N é um DTIM. DTIM não é outro mecanismo.
2. **Power save por DTIM, e o listen interval.** Quem manda no período é o AP. Com os números
   medidos na bancada: beacon interval 100 TU, DTIM 3, listen interval 10.
3. **TWT.** A estação negocia o próprio horário. O preço explícito: não acorda para o DTIM,
   logo não recebe broadcast nem multicast.
4. **Quando cada um ganha, e a dependência do AP.** DTIM para vazão e latência; TWT para
   dormidas longas com tráfego previsível. E o resultado medido: a ONT da bancada é 802.11ax e
   responde `Peer not TWT capable`. Não escrever "TWT é o moderno, DTIM é o legado".

Regras do toolkit (aprendidas nos M2-01/02/03): `**negrito**` e `` `mono` `` não aninham; cada
item de `add_cards`/`two_column` é **uma** string corrida; sem itálico.

- [ ] **Step 4: Construir e conferir por PNG**

```bash
cd doc/_template && python fig_m2_04_companion.py && python fig_m2_04_companion.py --deck
python fig_m2_04_transportes.py && python fig_m2_04_transportes.py --deck
python build_m2_04.py
```

Exportar os slides para PNG e revisar transbordo, crases literais e sobreposição.

- [ ] **Step 5: Commit nos dois repos**

```bash
git add doc/_template/build_m2_04.py doc/_template/fig_m2_04_*.py doc/comms/img
git commit -m "M2-04: deck de Wi-Fi 6+ com os numeros medidos na bancada"
```

(No repo de `doc/`, na branch **`wifi`**; os `.pptx` continuam ignorados.)

---

## Auto-revisão

**Cobertura da spec:**

| Seção da spec | Tarefa |
|---|---|
| §2.1 par LM20+EB II | Tasks 2–11 (todo build) |
| §2.2 blobs | Global Constraints + Task 12 |
| §2.3 armadilhas (console, sw3, mic) | Task 2 Step 5, Global Constraints, Task 13 Step 1 |
| §2.4 provisionamento | Task 4 |
| §2.5 TWT/coex/location | Tasks 1, 9, 10, 11 |
| §2.6 credenciais | Task 3 |
| §3.1 um payload, três transportes | Tasks 5, 6, 8 |
| §3.2 downlink separa os protocolos | Task 6 Step 3, Task 8 Steps 2 e 7 |
| §3.3 payload não é o estado do link | Task 5 |
| §3.4 o que cada lab novo mede | Tasks 9, 10, 11 |
| §3.5 plano B por lab | READMEs (Tasks 2–11) |
| §3.6 ORIGEM e licenças | Global Constraints, Tasks 2–4, 9, 10 |
| §4 os oito labs | Tasks 2–11 |
| §5 convenções | Global Constraints, Task 3 |
| §6 bancada | Task 12 Step 2 |
| §7 material | Task 13 |
| §8 validação | Tasks 1–11 (cada Step de bancada) + Task 12 Step 3 |

**Consistência de tipos:** `struct payload_amostra` e `payload_montar()` da Task 5 são usados
com os mesmos nomes na Task 6. `transporte_abrir/enviar/receber/fechar` da Task 6 são as mesmas
assinaturas estendidas na Task 8. `CONFIG_LAB_SERVIDOR_IP` e `CONFIG_LAB_PORTA` têm o mesmo
nome nos labs 9 e 13. `parse()` do `payload_ref` é consumido pelo `wifi_server.py` (Task 7).

**Sem placeholders:** todos os comandos, trechos de código e testes estão escritos por extenso.
Os três pontos que dependem de medida — nome real da imagem no sysbuild, FLASH/RAM, e se o MQTT
cabe na RAM — têm instrução explícita de o que fazer com cada resultado possível.
