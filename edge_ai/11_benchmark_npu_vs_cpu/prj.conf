#
# 11_benchmark_npu_vs_cpu (curso nrf-manaus-2). A parte de engine segue os
# prj_example.conf dos dois backends; os buffers do Axon ficam no conf de board
# (so valem quando CONFIG_NRF_AXON entra pela escolha do Kconfig).
#

# nRF Edge AI
CONFIG_NRF_EDGEAI=y
CONFIG_FPU=y

# Console e logging (desligados pelo power.conf na medicao de corrente)
CONFIG_LOG=y
CONFIG_CONSOLE=y
CONFIG_SERIAL=y
CONFIG_ASSERT=y
