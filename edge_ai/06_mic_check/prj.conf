#
# mic_check — prova de bancada do microfone PDM (curso nrf-manaus-2).
# So o que o driver de DMIC e o printk precisam; nada de Edge AI aqui.
#

CONFIG_AUDIO=y
CONFIG_AUDIO_DMIC=y

CONFIG_FPU=y

CONFIG_CONSOLE=y
CONFIG_SERIAL=y
CONFIG_UART_CONSOLE=y
CONFIG_LOG=y
